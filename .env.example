# Rename to .env.local and fill with your keys

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
# Signing secret from Stripe CLI/Dashboard for /api/stripe/webhook
STRIPE_WEBHOOK_SECRET=

# Optional: GitHub token to increase API limits
# GITHUB_TOKEN=

# Public site URL used by robots.txt & sitemap.xml (SEO)
# e.g., https://donate.example.com
NEXT_PUBLIC_SITE_URL=
